const path = require ("path")
const fs = require ("fs")

let products = require ("../data/products.json")


const controller = {
    index: (req, res)=>{
        res.render('index', )
    }
}
      
module.exports =  controller;


